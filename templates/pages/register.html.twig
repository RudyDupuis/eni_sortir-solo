{% extends 'base.html.twig' %}

{% block title %}Inscription {{ parent() }}{% endblock %}
{% block css %}<link rel="stylesheet" href="{{ asset('style/authentication.css') }}">{% endblock %}

{% block body %}
    <a href="{{ path('outing_home') }}"><img src="{{ asset('images/logo.png') }}" alt="Logo de l'ENI" class="logo"/></a>

    {{ form_start(registrationForm, {'attr': {'class': 'f-col a-cent j-even form'}}) }}

    {{ form_row(registrationForm.firstName, {
        'label': false,
        'attr': {
            'placeholder': 'Prénom',
            'class': 'mb-32'
        },
    }) }}
    {{ form_row(registrationForm.lastName, {
        'label': false,
        'attr': {
            'placeholder': 'Nom',
            'class': 'mb-32'
        },
    }) }}
    {{ form_row(registrationForm.email, {
        'label': false,
        'attr': {
            'placeholder': 'Email',
            'class': 'mb-32'
        },
    }) }}
    {{ form_row(registrationForm.plainPassword, {
        'label': false,
        'attr': {
            'placeholder': 'Mot de passe',
            'class': 'mb-32'
        }
    }) }}
    {{ form_row(registrationForm.campus, {
        'label': false,
        'attr': {
            'class': 'mb-32'
        }
    }) }}

    <button type="submit">S'inscrire</button>
    {{ form_end(registrationForm) }}
{% endblock %}
